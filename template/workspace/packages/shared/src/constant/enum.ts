export enum ModelType {
  user = 'user',
}

export enum FileType {
  images = 'images',
  pdf = 'pdf',
}

export enum ObjectStatus {
  Active = 'active',
  Archived = 'archived',
  Pending = 'pending',
  Canceled = 'canceled',
  Deleted = 'deleted',
}

export enum SortDirection {
  Asc = 'ASC',
  Desc = 'DESC',
  None = 'NONE',
}
